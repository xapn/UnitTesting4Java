/**
 * geometry package lets exemplify the refactoring step of the TDD cycle.
 * The creation of the geometry package and the decorator design pattern
 * implementation would be useful if one is willing to develop several solutions
 * of GPS.
 */
package xapn.projects.tdd.car.v3.geometry;